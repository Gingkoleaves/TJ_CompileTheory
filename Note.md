# 编译原理

## 2026年4月7日

### 高级语言与语法描述

二义性体现在，存在句型有多个语法树；注意允许且一定存在左右推导顺序不同【应用产生式的顺序不用】但是无二义性要求虽然应用产生式的顺序不同，但是语法树一样且唯一
二义文法的改造：引入优先级
文法的优先级通过规定允许发生对应操作的非终结符来保证：

E ->T | E×T
T ->F | T+F
F ->( E ) | i

这样规定x只能发生在E，+只能发生在T

### 词法分析

词法分析的任务是：从左至右逐个字符地对源程序进行扫描，产生一个个单词符号，把作为字符串的源程序改造成为**单词符号串**的中间程序
具体来说，形成**若干(type,value)二元组**，例如(liter字面量，123)，附加位置信息line-3-col-1;最终形成**符号表**，登记所有出现的符号、值的存储地址、type等信息

type一般包括 标识符、常熟、关键字、运算符、界符【,;(){}[]】

Lexer通常需要预处理空白符号、tab、回车、注解等；符号识别时通常需要peek超前搜索，识别不同type可以用不同状态转换图【获取一段前后空格分隔的字符串，逐个type尝试匹配状态转换图】，状态转换图可以对应到具体的if-else或while语句，终态节点对应return(type,value)语句

![Scanner Example](assets/image.png)

正规式和正规集：前者是匹配规则，后者是具体句子【(a|b)(a|b)正规式，正规集{aa,ab,ba,bb}】
L(正规式)表示它识别的集合

简化DFA：从全体终结态和非终结态两个集合开始，每次看对于一个输入a，各集合的各状态到达的下一个状态的终结与否是否一致，如果不一致则划分；对于**有限状态**机，总是能划分结束的

LEX程序由一组正规式以及与每个正规式相应的动作组成，动作本身是一小段程序代码，它指出了当按正规式识别出一个单词符号时应采取的行动。
Lex程序【辅助定义式【定义简名和正规式的对应】，正规式-actions【action是程序，匹配正规式时传入字符串，action最终返回二元组】】通过 词法分析程序自动产生器 获得词法分析器L，L可以对输入串处理形成若干前述二元组

LEX编译程序的具体操作：对每条识别规则Pi构造一个相应的 NFA Mi，引入一个新的初态X, 连接成 NFA M
用子集法将其确定化并化简，将 DFA 转换为词法分析程序【每个终态对应某个action，处理匹配的字符串并输出二元组】

### 语法分析：自上而下

语法分析的任务 ：对任一给定 w ∈ VT\*，判断是否 w ∈ L(G)；**产生语法树**并**验证符号串是否被本语言文法接受**
自上而下分析【尝试推导到符号串】： LL（1）分析法， 递归下降分析法， 预测分析法
自下而上分析【尝试规约带开始符】： 算符优先分析法， LR分析法

自上而下分析的问题：
首先，自上而下分析存在**回溯**，如果一个文法是含有左递归的，存在非终结符P：P ⇒Pα，则自上而下的分析过程陷入无限循环
LL(1)分析法：从左(Left)到右扫描输入串；构造最左(Leftmost)推导；分析时每步向前看一个(1)字符

> 左递归文法
> ![如果一个文法是左递归时，则不能采用自顶向下分析法](assets/image-1.png)

如何消除左递归：![直接左递归的处理方法：左递归改为右递归](assets/image-2.png)
**对每个非终结符，希望它的右边不要再出现编号比它小的非终结符作为第一个符号**
改写所有形如Pi->Pja的产生式，每次处理建立在上次处理之上，实际上消除了若干中间状态

![具体操作算法](assets/image-3.png)
把所有非终结符编号，从小到大遍历i，把所有j < i的j，在i->j中都把j打开，然后消除直接左递归和多余产生式

![实践案例](assets/image-4.png)

我们希望减少回溯，如果能实现对于任意非终结符A，对于指定的终结符a，至多找到一个路径W，使得从A经过W可能到达a..的终结符串，则可以避免回溯【如果有这样路径，至多一个路径，直接跳转；如果没有，直接考虑下一种匹配type】

## 2026年4月16日

### 语法分析：自上而下 continue

回溯问题：指的是给定peek到的字符ch和当前非终结符A，存在复数个产生式，可以导致A->ch ...,此时需要对这些产生式逐一尝试
回溯问题产生的来源是文法产生式的形式：可以验证文法是否是无回溯的【回溯可以视为左递归的下位情况，也是需要避免的】

无回溯的文法要求：
![符号串的FIRST定义](assets/image-9.png)
![无左递归，各产生式右侧的FIRST不交](assets/image-5.png)
回溯的解决办法：
提取公共左因子为中间变量，从中间变量实现无回溯；注意要一直找到所有右侧式的首符为终结符再提取中间变量
![图示](assets/image-6.png)
![图示2](assets/image-7.png)

FOLLOW:后继终结符号集
![对非终结符定义，从S开始可能出现在A后的终结符的集合](assets/image-8.png)

无回溯除了对各产生式右侧首终结符不交，还要求当存在A->ε时，FOLLOW(A)与这些首终结符也不交
![LL(1)文法三个要求](assets/image-10.png)
![如果有LL(1)文法，对输入串可以无回溯地、确定地展开【从S展开】分析](assets/image-11.png)

FIRST与FOLLOW的构造
![FIRST构造：考虑非终结符右侧子串可以导出ε的情况](assets/image-12.png)
![FOLLOW构造：注意FOLLOW没有ε，而是用#表示匹配结束](assets/image-13.png)

递归下降分析程序：根据LL(1)得到每个非终结符的if-else结构的，不会死循环的程序
![程序形式：ch是peek到程序](assets/image-14.png)
可以加入advance/read表示扫描字符串的指针前移，ptr从第一个字符开始，P(A)表示从包含当前ptr位置开始匹配;adv/read出现在对终结符的匹配

预测分析程序：使用一个分析表和符号栈进行联合控制，避免递归
![构成：栈、分析表、字符串](assets/image-17.png)
![分析表的构成:因为follow中包括#](assets/image-18.png)
可以视为是一个特殊的PDA下推自动机，只有一个非终结状态【每次状态转移只由栈顶和当前字符决定】和一个终结状态
![程序流程：压栈#和S开始符，直到a==x==#时成功](assets/image-16.png)
预测分析程序的正确性：每个表格框中只有一个表达式

构造分析表：把产生式填入表格
![分情况讨论，A->a和A->ε](assets/image-19.png)

### 语法分析：自下而上

移进-归约法：使用一个符号栈，把输入符号从左到右逐一移进栈，当栈顶形成某个产生式右部时，则将栈顶的这一部分替换（归约）为该产生式的左部符号

问题：如果存在不同左部推导相同右部，如何选择替换的左部

短语：b是句型abc相对于非终结符A的短语，指非终结符A在以某一个符号串b为推导目标时，S可以得到一个句型abc【即当A->b时，S存在一条路径得到abc】
![定义](assets/image-20.png)
![图示案例](assets/image-21.png)

用语法树来解释短语、直接短语和句柄
![注意直接短语的子代是叶子结点](assets/image-22.png)

二义性文法的句柄可能不唯一：因为语法树不唯一
![案例](assets/image-23.png)
二义性文法可以通过设置优先级消除二义性，或者通过固定操作策略【Leftmost/Rightmost】虽然是二义性文法得到唯一树

规范规约：移进-归约法的规范性描述
![规范规约的过程：依赖语法树](assets/image-24.png)

## 2026年4月30日

用符号栈实现移进-归约分析：两个#内表示右侧移动到左侧
![案例](assets/image-25.png)

提出语法树的一种可视化表示：穿线表
![语法符号-child数-一系列child指针](assets/image-26.png)

算符优先分析方法：按照终结符的优先关系进行规约
算符文法定义：![产生式右侧不含两个并列非终结符](assets/image-27.png)
利用算符文法定义终结符优先级【注意没有交换律，且可能出现a< b且a>b】：![描述的是推导过程中可能的ab出现的顺序，小于更先](assets/image-28.png)
算符优先文法：如果任意一对终结符(a,b)至多满足一个<,=,>,则称为算符优先文法【注意没有交换律！a< b且b< a也是算符优先文法】

定义FIRSTVT和LASTVT ![定义](assets/image-29.png)
![利用FIRSTVT和LASTVT描述终结符的优先关系](assets/image-30.png)
如何构造FIRSTVT和LASTVT：![定义构造和递归扩充](assets/image-31.png)

FIRSTVT具体构造方法：
FIRSTVT通过一个非终结符为行，终结符为列的矩阵，[P,a]表示a属于FIRSTVT(P)与否，反复填表直到表不产生变化：![先赋值初始值，然后逐个[Q,a]检查是否存在产生式P->Q，如果存在则[P,a]=true](assets/image-32.png)【注意FIRSTVT有两种定义】
LASTVT构造方法类似

优先关系表构造方法：形成终结符\*终结符的矩阵
![具体操作流程](assets/image-33.png)

定义 素短语：至少包含一个终结符的最小短语
![最左素短语](assets/image-34.png)
算符优先文法的最左素短语寻找方法【给定一个句型，如何找出最左素短语】 ![用=保证没有更小的素短语](assets/image-35.png)
![具体操作方法](assets/image-36.png)

对算符优先文法，使用最左素短语进行规约 ![算符优先分析方法](assets/image-37.png)
每次先写出符号栈和输入串，标注栈顶首个终结符和串首符号的关系；在关系为>时可以写出最左素短语；如果能写出，则在下一行先规约最左素短语，再判断关系
左#的优先级比所有的终结符低，右#也是，他们俩=；最后右侧#移动到符号栈形成#N#结束

## 2026年5月9日

算符优先分析：对于终结符，总是规约为N【表示若干符号作为一个整体】，只关心句型内的终结符，所有非终结符实际上视为相同的N【不关心非终结符为右部的产生式】；最后#N#视为成功
![分析过程](assets/image-38.png)

算符优先分析实际上只关心算符之间的规约，无法处理E->F非终结符之间的单到单产生式；所以算符优先分析实际上把算符之间的若干非终结符视为一个整体N，构造语法树
![算符优先【只关心算符之间的优先级】的语法树和规范规约【真正使用每条产生式】语法树](assets/image-39.png)
这里的“误判”指的是可能吧本身不为句子的串识别为句子

优先函数：每个终结符维护两个值f,以此代替优先表【可以从fg恢复优先表，优先函数需要的存储空间更少】
![希望对于有优先级的两个终结符，根据优先关系有f、g满足一下关系](assets/image-40.png)
有许多优先关系表不存在优先函数；如果优先函数存在，则可以通过以下三个步骤验证存在，并从优先表构造优先函数
![逐个终结符遍历，用有向边描述上述fg关系，->表示左侧f/g大于右侧f/g](assets/image-41.png)
算符优先分析算法只适用于算符优先文法，只能对算符优先文法生成文法树，从而检查语法正确性【误判案例：对于S->(S),S->e,()会被视为合法，但实际上不合法；**它只检查算符的先后顺序是否符合优先级规则，而不检查这些算符之间是否按规则填充了必要的非终结符**】

LR分析法：LR分析法中的L表示从左向右扫描输入串， R表示构造最右推导的逆。LR分析法是严格的规范规约
![文法产生分析表，分析表控制总控程序](assets/image-42.png)
分析表包括LR(0)、SLR、规范LR、LALR；包括ACTIONS和GOTO两张表，ACTION[s,a]描述状态s面临符号a时的动作，GOTO[s,X]描述状态s面对符号X是下一个状态

分析过程维护一个状态栈，一个符号栈，一个输入串队列，分别以(S0,#,符号串)为初始值；每步动作为ACTIONS[栈顶状态，输入串首符号]
![ACTIONS有四种，根据这里定义的操作更新三元式](assets/image-43.png)

LR文法: 对于一个文法，如果能够构造一张LR分析表, 使得它的每个入口均是唯一确定，则该文法称为LR文法
LR(k)文法：对于一个文法，如果每步至多向前检查 k个输入符号，就能用LR分析器进行分析。则这个文法就称为LR(k)文法
LR(0) 文法：k ＝0，即只要根据当前符号和历史信息进行分析，而无需展望
这里k的长度是ACTIONS[S,a]中a作为符号串的长度，即需要往前看多少个符号串才能决定动作

## 2026年5月26日

语义分析和中间代码产生
中间语言的表示方法：后缀式/逆波兰式，图表示，三地址

后缀式不用括号，可以实现无二义性的计算![具体表示方法](assets/image-44.png)
如何从产生式得到语义规则：![描述如何从语法树开始，通过语义制导翻译得到根的属性，从而得到全文的后缀式](assets/image-45.png)
工程实现：不会按照属性计算反复拷贝字符串，而是维护全局POST；通过LR分析[自底向上]时，等价于对语法树后序遍历，而逆波兰式带来的语义规则中，属性code的赋值也是按照左子+右子+根的顺序；这保证了在使用LR分析时，最终根的code属性中子节点code的顺序，恰好是LR分析时逐个子结点处理的顺序，所以可以直接维护一个全局POST，每次处理语义规则时后缀链接新符号
![具体案例](assets/image-46.png)

图表示法[描述IR的方式]：DAG、抽象语法树
![DAG：用操作符做内部节点，终结符为叶子节点](assets/image-47.png)
抽象语法树：简化语法树中与翻译无关的信息
用一下三种操作来构成语义规则![给出三种create-node的操作](assets/image-48.png)
一般的语法树CST通过语法分析形成，记录文法产生式如何匹配各token；在形成CST过程中通过语义制导翻译[一边匹配CST一边计算属性]得到AST[由指针链接的，简化结构的语法树节点]；实际上CST不会在内存中建立，编译器在规约的过程中不生成CST实体，而是只计算得到AST[在内存中建立]
最终的效果是，parser的符号栈中留下根节点E，根节点E保存一个指针nptr，指向AST的根节点；AST-root中存储子节点的nptr
IR有多种表示方法，DAG相比AST减少了相同节点的重复存储，注意这里所有链接都是有向边![DAG和AST表示法](assets/image-49.png)

三地址代码：可以看成是抽象语法树或DAG的一种线性表示；是在讨论当已经存在一个AST时，如何生成线性的三地址语句序列
![三地址语句种类：relop是关系运算符<=>](assets/image-50.png)
![描述三地址语句对应的语义规则](assets/image-51.png)
通过四元式/三元式来记录三地址语句，并从上到下计算[前一句的result进入下一句]
![三地址语句的存储方式](assets/image-52.png)
间接三元式也是前一result进入下一句，但是允许自定义计算顺序，避免必须按照表中顺序运行[从而更改运算顺序更灵活，因为三元式在表中硬编码了编号，单个移动导致全局编号修改]

可见，上述三种方法实际上对应三种从产生式得到语义规则的映射；通过不同的映射规则，得到不同的语义规则集合，并在SDT中应用，就得到了不同的IR

## 2026年5月28日

说明语句的翻译：包括定义标识符和类型
常量定义的翻译：涉及常量表和符号表；前者存储值，后者存储常量标志、类型和常量表序号
kind字段标记可变与否，即var或const
![常量定义的翻译示例](assets/image-44.png)
变量说明的翻译：在规约过程中在内存中分配变量存储空间
offset表示从可分配的内存空间开始的偏移量，一般表示[0,offset]的内存已经被占用
在说明前，设置offset=0；过程中根据变量类型移动offset，并把起始位置同变量类型和标识符名写入符号表[enter动作]
![变量说明的翻译](assets/image-45.png)

过程调用的翻译：每个过程应当维护独立的符号表，以实现作用域隔离；这通过在进入过程前引入可以直接规约的M来实现
![过程调用涉及的语义规则](assets/image-46.png)
enter和enterproc都是在指定的符号表中建立新项，后者在父过程的符号表中存储proc-id到过程符号表的映射
其他规则：push(t,tblptr) 把新表指针 t 压入符号表栈 tblptr 的栈顶，调用 top(tblptr)可以拿到这个新环境的专用表；此外还有其他表例如push(0,offset)的offset栈
![过程调用翻译的产生式与语义规则对应](assets/image-53.png)
这里的M是全局的初始符号表，N产生各个子过程的符号表，区别是只有后者存储父过程的符号表

赋值语句的翻译：![涉及的语义规则](assets/image-54.png)
![给出AST和SDT两种三地址代码表达方式](assets/image-55.png)
.code存储三地址代码，最终在root聚合为全体文本的三地址代码；.place指向符号表中的变量/常量表项，描述属性来源【注意code是操作流程，place是变量的占位符，变量的值在code过程中得到并写入place；~~这里code和place在考虑算术操作时明显重复[E=b+c,E.place对应的临时变量空间存储b+c,而E.code也存储b.code||+||c.code]，但是在非算术场景下没有重复，因为code是操作，place此时只等待code写入~~】；newtemp在符号表中新增一项
这二者不同在于产生三地址代码是逐步的还是一次性的；ast方式反复cp代码直到root一次性产出；sdt边扫描边产出emit【依赖后序遍历保证顺序二者一致】；这是两种不同的产生式->语义规则的映射，可以视为emit E.code减少存储压力

布尔表达式的翻译：布尔运算由高到低 :not and or ，同级左结合；关系运算符同级，且高于布尔
布尔表达式[与或非、relop]有使用临时变量[if-else语句和直接emit and/not/or]和使用跳转语句[维护true/false两种属性]两种翻译方法：![布尔表达式与或非](assets/image-62.png)
![bool表达式涉及的语义规则](assets/image-57.png)
数值表示法的翻译模式：and/or/not和relop,使用临时变量
![bool表达式的产生式和语义规则映射：直接计算形式的翻译](assets/image-58.png)

> 注意这里应该说明a< b是关系表达式relop-op；关系表达式直接翻译为[三地址代码如下] id1.place relop.op id2.place,视为一个bool值;但是当出现if relop else时，应当视为一个整体进行翻译

![条件控制翻译的一般结构：if [bool] goto [code1] goto [code2]](assets/image-59.png)
注意！以下label是继承属性，表示当确定本项为T/F时跳转到哪个label执行；继承属性意思是获取布尔表达式外的跳转地址，这也是合理的[bool本身就是用于抉择选择外部哪一条路]
![bool表达式的产生式和语义规则映射：if-else跳转形式的翻译](assets/image-60.png)
![上图是or，这里是and、not](assets/image-64.png)
![这里是relop和true/false](assets/image-65.png)
注意E.code中的gen(E.false':')就使用了E1.false=newlabel产生的新label，表示当E1为false时跳转到这个E1.false处继续执行，也即执行E2.code
E.code中使用E.true和E.false，所以可以通过修改和继承.false\.true来更改code行为
一般relop使用条件控制的翻译方法

![使用数值表示法方式翻译：](assets/image-63.png)
上图是语义规则和对应的emit输出；为什么不拆分relop关系表达式和bool表达式？因为硬件只支持用过relop实现if-else，不实现if-bool-else,只实现if-relop-else [这在三地址代码定义中直接包括if-relop-else而不是if-bool-else也可以体现]
可见if-else跳转形式的翻译依赖.true\.false属性和label；而直接计算形式的翻译通过.place反复传递计算结果
![条件控制的翻译方式](assets/image-61.png)

## 2026年6月11日

布尔表达式翻译：两遍扫描和一次扫描
一次扫描：在需要跳转目标但是不知道时，需要为非终结符维护truelist属性，表示跳转链条最末应该填入的跳转目标![图例](assets/image-66.png)
![处理truelist需要引入的过程和变量](assets/image-67.png)，merge意思是二者的true/false应该填入相同的目的地
在向上的过程中，解决可知的truelist、falselist[短路运算]，把不可知的向上传递；![布尔表达式的翻译](assets/image-68.png)
![附图2](assets/image-69.png)
其实还应该维护nextlist

控制语句的翻译：if-then、if-then-else、while-do、go-to[也用链维护]、case
承接上述bool表达式的true-false出口，真实回填：![具体翻译方式](assets/image-70.png)
还包括若干相同出口的统一工作merge
![goto和label语句](assets/image-71.png)
![case语句翻译两种方法](assets/image-73.png)

过程调用的翻译
![维护参数表，并转化为par列](assets/image-74.png)

chap8、9、10、11

chap9：运行时存储空间

目标程序运行时的活动
过程：过程名+过程体，过程的活动指它的一次执行
参数传递：传地址、得结果、传值、传名
得结果是传地址的一种变形：同时维护函数内局部变量[值拷贝]和指向他们的传入实参的地址，结束时根据参数顺序把函数内局部变量写回实际参数
传名相当于宏替换，与传地址的区别是，传地址可以传入临时变量[例如在传地址时，F(A+B)实际上不会影响A和B，而是影响一个临时变量A+B]

运行时存储器的划分
目标代码运行所需空间：目标代码存储、临时数据存储、控制栈
code-static_data-stack-mmap_space-heap
活动记录：存储函数调用的context信息，实际上就是栈帧stackframe？
![活动记录内容](assets/image-75.png)
返回地址
动态链：指向调用该过程前的最新活动记录地址的指针，runtime的调用者
静态链：指向静态直接外层最新活动记录地址的指针，用来访问非局部数据，编译时决定的外层函数地址【c不支持所谓的嵌套函数】
形式单元：存放相应的实在参数的地址或值。
局部数据区：局部变量、内情向量、临时工作单元（如存放对表达式求值的结果）
对任何局部变量X的引用可表示为变址访问:   dx[SP]
dx: 变量X相对于活动记录起点的地址，在编译时可确定

静态分配策略(FORTRAN)： 如果在编译时能确定数据空间的大小，则可采用静态分配方法。在编译时刻为每个数据对象分配固定的存储单元，且在运行时始终保持不变
动态分配策略(PASCAL) ：如果在编译时不能确定运行时数据空间的大小，则必须采用动态分配方法。允许递归过程和动态申请释放内存：栈式动态分配，堆式动态分配A

FORTRAN的存储分配:静态分配
![总体存储结构：分别存储局部和全局](assets/image-76.png)
![局部数据区结构](assets/image-77.png)
![局部数据举例：以一个子程序为例](assets/image-78.png)
临时变量的地址分配：复用
临时变量的特点: 放中间结果, 定值一次, 引用一次
![用栈实现作用域的层次嵌套,反复复用地址](assets/image-79.png)

以c语言为例的简单栈式存储分配

## 2026年6月21日

chap9-runtime-store
finished

## 2026年6月24日

chap10-optimize

优化部分输入四元式，输出四元式；包括控制流、数据流分析和代码变换，要保证功能等价、开销减少

优化类型：局部优化、循环优化、全局优化
优化的种类：删除代码块中公共子表达式[用临时变量存储计算结果避免重复计算]、代码外提、强度消弱[对于循环计数量i相关的乘法，可以用临时变量和加法代替]、变换循环控制条件、合并已知量[可以在编译期计算得到的就直接计算]、复写传播[如果某个内容写入临时变量，之后再使用这个内容时可以使用临时变量；可以多轮处理]、删除无用赋值[配合复写传播，删除变量反复赋值时的中间变量]、删除归纳变量[对于保持Ti=4i,Tj=4j，使用ij时可以用TiTj代替]![图示](assets/image-80.png) 

局部优化：考虑划分基本块和在基本块内的优化
![基本块和活跃定义](assets/image-81.png)

基本块大概意思是有一串语句序列，总是顺序执行而不会从中间开始执行[由于跳转]，这个序列[尽量长，否则每个单语句都是]就是基本块
在一个基本块内通常可以实行下面的优化:

- 删除公共子表达式：用临时变量存储中间结果
- 删除无用赋值：删除赋值传递链
- 合并已知量：常量计算提前算好插入
- 临时变量改名：生命周期不重合的临时变量可以复用同一个名称
- 交换语句的位置：需要耗时计算的语句提前，避免流水线气泡
- 代数变换：二次方改为乘法，从调用pow函数改为单条cpu乘法指令

划分四元式程序为基本块的方法：
找入口语句：程序第一个语句，或jp的目标语句或条件转移语句后的语句
找基本块的结束：入口语句到下一个入口语句[不包括]/下一个jp转移语句[包括]/停止语句[包括] 是一个基本块
对于没有纳入某个基本块的语句，可以删除

流图：基本块的执行流，各基本块因转移语句或前后顺序关系而连接
![流图示例](assets/image-82.png)
DAG表示：叶子节点标记常数或标识符，内部节点标记运算符：![四元式的DAG，用节点旁的标记标识语义](assets/image-83.png)
![各类四元式的DAG图，区分为4类四元式](assets/image-84.png)
如何构造基本块的DAG：注意3型不会在父节点引入运算结果；接下来用标识A表示(临时)变量名，NODE(A)表示A变量的最新值节点[NODE()视为映射函数，输入是常数或变量名]；可以将常数也视为一种变量名来统一操作[实际上提供常数标记]
结点 理解为一次操作[3型]/一个量
![这里描述得较不具体；1是准备右侧操作数的结点；2大意是删除右侧常数计算的中间节点，并为计算结果的新常数创建结点；3大意是如果存在右式结果则直接在节点贴上左式标记/op标记即可；4大意是移动变量指针到最新值，把左侧名称贴到指定结点上，注意如果Node(a)是叶结点则不删除标识A](assets/image-85.png)
所谓合并已知量，就是寻找公共常数，与公共子表达式同理

> 在基本块外被定值并在基本块内被引用的所有标识符，就是作为叶子结点上标记的那些标识符。
> 在基本块内被定值并且该值在基本块后面可以被引用的所有标识符，就是DAG各结点上的那些附加标识符。

循环优化：对循环中的代码，可以实行以下优化

- 代码外提：引入循环不变运算[一个运算四元式，右侧值的定值点在循环外，即运算结果在循环内不变]和定值到达[变量A从d点定植到达u点，指在执行过程中存在从d->u通路且途中没有修改A]两个概念，代码外提指的是在流图中把循环不变运算移动到循环涉及的基本块外，用临时变量在循环涉及的基本块内使用；要考虑四元式提出后左式提前赋值是否影响循环体内[循环体内是否存在对左式的引用]
- 查找循环的不变运算：![常数、外部定植点、递归寻找；标记四元式为不变运算](assets/image-86.png)；确定所有不变运算后，对于满足下列要求的四元式，提到基本块外![代码外提算法：考虑 A在循环后一定不活跃/循环必经A 两种情况才可以外提](assets/image-87.png)
- 强度削弱：执行时间长的运算改为执行时间短的，原因包括运算本身复杂性和重复运算；例如二次方改为乘法，对循环内计数量的常数乘法改为临时变量累加常数，多次链式加法(AB不变，循环内计算A+(B+i))改为单次加法(循环外T=A+B，循环内T+i)；![强度削弱总结](assets/image-88.png)
- 删除归纳变量(变换循环控制条件)：![给出(基本)归纳变量的定义，即每次基本归纳变量改变后，归纳变量跟着改变，且二者改变前后符合相同的线性方程](assets/image-89.png)
- ![如何进行强度削弱和删除归纳变量](assets/image-90.png)找出所有基本归纳变量和他们的归纳变量，删除累加链条，删除基本归纳变量[可以用归纳变量代替基本归纳变量控制循环条件]
- 循环展开
- 循环合并

目标代码生成：输入四元式，输出目标代码；重点考虑以下问题：

- 如何使生成的目标代码较短
- 如何充分利用计算机的寄存器，减少目标代码中访问存贮单元的次数
- 如何充分利用计算机的指令系统的特点

代码生成器的输入是中间表示和符号表
目标代码一般有以下三种形式：
能够立即执行的机器语言代码，所有地址已经定位；
待装配的机器语言模块。执行时，由连接装配程序把它们和某些运行程序连接起来，转换成能执行的机器语言代码；
汇编语言代码。尚须经过汇编程序汇编，转换成可执行的机器语言代码。

目标代码的设计要考虑以下三个问题：
指令的选择要具有一致性和完备性，寄存器如何最优分配[NP完全问题]，如何调整指令的运行计算顺序

设计一种抽象的目标机器模型，具有多个通用寄存器，他们既可以作为累加器，也可以作为变址器；运算必须在某个寄存器中进行![在ADD、SUB、MUL、DIV之外，指定以下运算](assets/image-92.png)；含有四种类型的指令形式：![提供四种获得参数的方法](assets/image-91.png)

使用以上的目标代码机器翻译四元式，考虑充分运用寄存器[尽量复用寄存器中内容而不是从disk中Load，尽量减少最大同时使用的寄存器数量]
待用信息定义：![四元式j是另一个四元式i的变量X的待用信息](assets/image-93.png)
![待用/活跃消息更新流程：基本块内从后向前标记](assets/image-94.png)
每个变量和每个四元式维护一个(n,y)待用/活跃状态，待用信息指变量在基本块内下一次被读取的地方，活跃信息指基本块外是否还需要
使用它的方法是，从后向前标记，然后正向考虑；如果运行到某个四元式时，存在符号为(^,^)则可以遗忘这个符号【根据是否活跃和是否待用来决定寄存器的使用策略】

寄存器描述：在基本块内分配寄存器，块间把活跃变量从寄存器存回到主存单元
![RVALUE[R]={A,B}表示寄存器R中存储变量A、B的值；AVALUE同理，右侧R1指寄存器，A/M指地址；二者互为相反给定变量/地址给出地址/变量](assets/image-95.png)

代码生成算法：根据四元式生成上述目标代码，重点是寄存器分配
![getreg分配寄存器R[考虑不分配到右侧第二个操作数C的当前寄存器[避免LD B->R 覆盖C内容]]，如果R就是右侧第一个寄存器B，可以节省一个LD指令；最后清理两个表](assets/image-96.png)

寄存器分配：GETREG(i: A:=B op C) 返回一个用来存放A的值的寄存器：

- 尽可能用B独占的寄存器：要求1：B的RVALUE[Ri]={B}只包含B；要求2：B之后不会再被引用，或A=B是同一个标识符；满足两个要求时可以复用
- 尽可能用空闲寄存器
- 抢占用非空闲寄存器：这个寄存器中的值同时存储在主存中，且希望最远的未来才使用；图中描述过于混乱，不妨修改如下：对于非ABC的变量，如果只存在于Ri则需要存储到主存，并修改两个表RVALUE-AVALUE；对于A则什么都不用做，对于B、C考虑存储到主存，但B不清理AVALUE[B]={Mem,Ri}因为运算还需要【用这种只删除一个表表示临时通过Aalue使用M】
![寄存器分配流程](assets/image-97.png)
